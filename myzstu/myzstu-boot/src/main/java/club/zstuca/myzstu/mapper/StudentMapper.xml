<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.zstuca.myzstu.mapper.StudentMapper">
    <select id="findStudentById" parameterType="string" resultType="club.zstuca.myzstu.entity.Student">
        select openid, sid, edupw, ecardpw
        from user
        where openid = #{openid}
    </select>
    <insert id="doCreate"
            parameterType="club.zstuca.myzstu.entity.Student">insert into user(openid, sid, ecardpw, edupw, libpw, ssopw)
                                                              values (#{openid}, #{sid}, #{ecardPw}, #{eduPw}, "081639", "081639")</insert>
    <update id="doUpdate" parameterType="club.zstuca.myzstu.entity.Student">
        update user
        set sid=#{sid},
            ecardpw=#{ecardPw},
            edupw=#{eduPw}
        where openid = #{openid}
    </update>
    <delete id="deleteStudentById">

    </delete>
</mapper>